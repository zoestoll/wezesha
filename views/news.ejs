<!doctype html>
<html>
    <head>
        <% include inner_head %>
    </head>

    <body>
        <% include navbar %>
        <h1 id="about_head">Recent News</h1>
        <!-- Page Content -->
        <div id="container">
            <div class="row">
                <div class="col-lg-12">
                    <div class="list-group">
                      <!-- Render each blog post -->
                      <% posts.forEach((post) => { %>
                            <a href="/post/<%= post.id %>" class="list-group-item">
                                 <!-- Title -->
                                <h4 class="list-group-item-heading"><%= post.title %></h4>
                                <!-- Author -->
                                <p class="list-group-item-author">by <%= post.author %></p>
                                <!-- Body -->
                                <p class="list-group-item-text"><%= post.body %></p>
                                <!-- Date/Time -->
                                <p><span class="glyphicon glyphicon-time"></span> <%= post.timestamp %></p>
                            </a>
                      <% }) %>
                    </div>
                </div>
            </div>
        </div>
        <% include footer %>
    </body>
</html>
